def maior_elemento(pilha):
    maior = pilha[0]
    for i in range(len(pilha)):
        if pilha[i] > maior:
            maior = pilha[i]
    return maior

p = maior_elemento([1,3,7,5,4])
print(p)